public class LargestPalindromeProduct {
    /*
        91 x 99 = 9009

        they need the largest palindrome number from two- 3 digits numbers

        a palindrome is a number that can be read the same forwards and the same way backwards

        with three digits could be

        1 x 999 where the first num will be the one to be incrementing?
     */
    public static void main(String[] args) {
//
//        String[] numbers = new String[5];
//        // getting a palindrome with two 3 digits numbers.
//        for (int i = 1; i <= 999; i++) {
//            int currentValue = i * 999;
//
//            for (int j = 0; j <= currentValue; j++) {
//                numbers[i]
//            }
//        }
    }
}
